[phases.setup]
nixPkgs = ["nodejs_20", "npm-10_x"]

[phases.build]
cmds = [
  "npm install",
  "npx prisma generate",
  "npx prisma db push",
  "node src/config/seed.js"
]

[start]
cmd = "node src/index.js"
